[phases.setup]
nixPkgs = ['nodejs', 'python311']

[phases.install]
cmds = [
  'pip install -r requirements.txt',
  'npm install'
]

[phases.build]
cmds = [
  'npm run build',
  'python manage.py collectstatic --noinput'
]

[start]
cmd = 'gunicorn doc_clinic.wsgi:application --bind 0.0.0.0:$PORT'